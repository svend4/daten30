# 🚀 Сборка Flutter приложения для Hub Portal

## ⚡ Быстрый старт (автоматическая сборка)

### Одна команда для полной автоматической сборки:

```bash
cd ~/daten30/hub-portal/flutter-hub
bash auto-build.sh
```

**Этот скрипт:**
- ✅ Автоматически установит Flutter (если нет)
- ✅ Настроит все зависимости
- ✅ Создаст необходимые файлы
- ✅ Соберет APK
- ✅ Скопирует APK с удобным именем

**Результат:** `DynamicHub-release.apk` готов к установке!

---

## 📋 Три способа сборки

### 1. Автоматическая сборка (рекомендуется)

```bash
cd ~/daten30/hub-portal/flutter-hub
bash auto-build.sh
```

**Преимущества:**
- Работает даже без установленного Flutter
- Автоматически установит Flutter в Termux или Linux
- Настроит все зависимости
- Создаст готовый APK

---

### 2. Стандартная сборка (если Flutter установлен)

```bash
cd ~/daten30/hub-portal/flutter-hub
bash build-apk.sh
```

**Требования:**
- Flutter должен быть установлен
- Android SDK настроен

---

### 3. Ручная сборка

```bash
cd ~/daten30/hub-portal/flutter-hub

# Установить зависимости
flutter pub get

# Собрать APK
flutter build apk --release

# APK будет в: build/app/outputs/flutter-apk/app-release.apk
```

---

## 📱 Установка APK

### В Termux

```bash
# После сборки APK скопировать в Downloads
cp DynamicHub-release.apk ~/storage/downloads/

# Открыть файловый менеджер и установить APK
```

### Через ADB

```bash
adb install DynamicHub-release.apk
```

---

## 🎯 Полный workflow

### Шаг 1: Клонировать репозиторий (если еще не сделано)

```bash
cd ~
git clone <URL> daten30
cd daten30
```

### Шаг 2: Собрать Flutter приложение

```bash
cd hub-portal/flutter-hub
bash auto-build.sh
```

Дождитесь завершения сборки (5-15 минут при первой сборке).

### Шаг 3: Установить APK

```bash
# В Termux
cp DynamicHub-release.apk ~/storage/downloads/
# Затем установить из файлового менеджера

# Или через ADB
adb install DynamicHub-release.apk
```

### Шаг 4: Запустить backend

```bash
cd ~/daten30/hub-portal
bash scripts/start-all.sh
```

### Шаг 5: Открыть приложение

Приложение автоматически обнаружит все запущенные сервисы!

---

## 🔍 Проверка сборки

### Проверить что APK создан

```bash
ls -lh DynamicHub-release.apk
# Должен показать файл размером ~20-30 MB
```

### Проверить Flutter

```bash
flutter doctor -v
```

---

## ⚙️ Требования

### Минимальные требования

- **Android:** 7.0+ (API 21+)
- **RAM:** 2 GB для сборки
- **Место на диске:** 1-2 GB для Flutter SDK

### В Termux

```bash
# Установить git и wget (если нет)
pkg install git wget
```

### На Desktop Linux

```bash
# Установить git (если нет)
sudo apt install git

# auto-build.sh установит Flutter автоматически
```

---

## 🐛 Troubleshooting

### Ошибка: Flutter not found

**Решение:** Используйте `auto-build.sh` - он автоматически установит Flutter

### Ошибка: Android licenses not accepted

```bash
flutter doctor --android-licenses
# Нажимайте 'y' на все вопросы
```

### Ошибка: Build failed

```bash
# Очистить и пересобрать
flutter clean
flutter pub get
flutter build apk --release
```

### Слишком долгая сборка

**Первая сборка:** 10-15 минут (загрузка зависимостей)
**Последующие сборки:** 2-5 минут

---

## 📊 Что входит в проект

```
hub-portal/flutter-hub/
├── lib/
│   └── main.dart                         # Flutter код (600+ строк)
│
├── android/                              # Android конфигурация
│   ├── app/
│   │   ├── build.gradle                  # Gradle конфигурация
│   │   └── src/main/
│   │       ├── AndroidManifest.xml       # Манифест с INTERNET
│   │       ├── kotlin/MainActivity.kt    # Main activity
│   │       └── res/
│   │           ├── xml/
│   │           │   └── network_security_config.xml  # HTTP доступ
│   │           └── values/styles.xml
│   ├── build.gradle
│   ├── settings.gradle
│   └── gradle.properties
│
├── pubspec.yaml                          # Зависимости
├── auto-build.sh                         # ⭐ Автоматическая сборка
├── build-apk.sh                          # Стандартная сборка
├── AUTO_BUILD_README.md                  # Подробная документация
├── BUILD_INSTRUCTIONS.md                 # Инструкции сборки
└── README.md                             # Описание приложения
```

---

## 🎨 Что делает приложение

### Dynamic Hub Portal

**Динамическое Flutter приложение**, которое:

1. **Автоматически обнаруживает** все микросервисы через Service Registry (порт 5000)
2. **Генерирует UI** из JSON schemas, которые отправляет каждый сервис
3. **Отображает сервисы** как карточки на главном экране
4. **Обновляется** каждые 30 секунд - новые сервисы появляются автоматически!

### Преимущества

- ✅ Не нужно пересобирать APK для добавления новых функций
- ✅ Просто запустите новый микросервис - он появится в приложении
- ✅ Plugin architecture - добавляйте функциональность динамически
- ✅ Работает с любыми микросервисами, отправляющими UI schema

---

## 📚 Дополнительная документация

| Документ | Описание |
|----------|----------|
| [AUTO_BUILD_README.md](hub-portal/flutter-hub/AUTO_BUILD_README.md) | Подробная документация auto-build.sh |
| [BUILD_INSTRUCTIONS.md](hub-portal/flutter-hub/BUILD_INSTRUCTIONS.md) | Детальные инструкции сборки |
| [FLUTTER_APPS_COMPARISON.md](FLUTTER_APPS_COMPARISON.md) | Сравнение трех Flutter приложений |
| [hub-portal/README.md](hub-portal/README.md) | Архитектура Hub Portal |
| [TERMUX_QUICK_START.md](TERMUX_QUICK_START.md) | Быстрый старт в Termux |

---

## 💡 Следующие шаги

После сборки и установки APK:

1. **Запустите backend в Termux:**
   ```bash
   cd ~/daten30/hub-portal
   bash scripts/start-all.sh
   ```

2. **Проверьте что сервисы запущены:**
   ```bash
   bash scripts/health-check.sh
   ```

3. **Откройте приложение на телефоне**

4. **Наслаждайтесь динамическим интерфейсом!**

---

## 🌟 Основные команды

| Команда | Описание |
|---------|----------|
| `bash auto-build.sh` | Автоматическая сборка с установкой Flutter |
| `bash build-apk.sh` | Стандартная сборка (Flutter уже установлен) |
| `flutter pub get` | Обновить зависимости |
| `flutter clean` | Очистить кэш сборки |
| `flutter doctor -v` | Проверить Flutter |
| `adb install <APK>` | Установить APK через ADB |

---

**Создано:** 2026-01-06
**Версия:** 1.0
**Проект:** Daten30 Dynamic Hub Portal

**Готовность:** ✅ Полная автоматическая сборка работает!
