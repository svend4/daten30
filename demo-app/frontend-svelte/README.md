# 🚀 Demo App - Svelte Frontend

Frontend приложение на Svelte для демонстрации минималистичного подхода.

## Философия

**Svelte компилируется в vanilla JavaScript** — нет runtime библиотеки!
- Runtime size: **0 KB** (компилируется в чистый JS)
- Bundle size: **~2-3 KB** (gzipped)
- Performance: **Отличная** (нет Virtual DOM)

## Технологический стек

- **Svelte 4.2** — компилирующий фреймворк
- **Vite 5.0** — быстрый build tool
- **Vanilla CSS** — никаких CSS фреймворков

## Особенности

✅ Реактивность без runtime
✅ Компиляция в оптимизированный JS
✅ Минимальный bundle size
✅ Отличная производительность
✅ Простой и читаемый код

## Разработка

### Установка зависимостей

```bash
npm install
```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### Production сборка

```bash
npm run build
```

Результат в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## Docker

### Сборка образа

```bash
docker build -t demo-app-svelte .
```

### Запуск контейнера

```bash
docker run -p 8081:80 demo-app-svelte
```

Приложение доступно на `http://localhost:8081`

## Структура проекта

```
frontend-svelte/
├── src/
│   ├── main.js              # Entry point
│   ├── App.svelte           # Главный компонент
│   ├── lib/
│   │   └── api.js           # API клиент
│   └── components/
│       ├── Dashboard.svelte # Dashboard
│       ├── Users.svelte     # Список пользователей
│       ├── Products.svelte  # Список товаров
│       ├── Orders.svelte    # Список заказов
│       └── Analytics.svelte # Аналитика
├── index.html
├── vite.config.js
├── package.json
└── Dockerfile
```

## API Endpoints

Приложение подключается к следующим endpoints:

- `GET /api/users` — список пользователей
- `GET /api/products` — список товаров
- `GET /api/orders` — список заказов
- `GET /api/analytics/summary` — аналитическая сводка
- `GET /api/analytics/daily` — дневная статистика
- `GET /api/user-stats` — статистика пользователей
- `GET /api/product-stats` — статистика товаров
- `GET /api/order-stats` — статистика заказов
- `GET /api/health/{service}` — health checks

## Сравнение с другими фреймворками

| Фреймворк | Runtime Size | Философия |
|-----------|--------------|-----------|
| **Svelte** | **0 KB** | Компиляция в vanilla JS |
| Alpine.js | 15 KB | Декларативный HTML |
| Preact | 3 KB | React-совместимый |
| SolidJS | 7 KB | Реактивность без VDOM |
| React | 43 KB | Virtual DOM |
| Vue | 34 KB | Progressive framework |

## Production

В production рекомендуется:

1. Использовать Nginx для раздачи статики
2. Настроить gzip compression
3. Добавить cache headers
4. Использовать CDN для статических ресурсов

## Минимализм в действии

Svelte следует философии **композиции** и **минимализма**:
- Компоненты композируются из более мелких
- Нет лишнего runtime кода
- Оптимальный bundle size
- Читаемый и простой код

**Минимализм работает!** 🚀
