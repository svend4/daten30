name: demo_app_mobile
description: Flutter mobile app for Demo App - Идеальный Стек

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP клиент
  http: ^1.1.2

  # State management
  provider: ^6.1.1

  # UI компоненты
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
