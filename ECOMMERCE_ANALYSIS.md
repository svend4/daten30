# 🛒 Анализ интернет-магазина и план развития

**Дата:** 2026-01-06
**Проект:** daten30 - Flutter Mobile App + Termux Backend
**Текущая версия:** 1.0.0 (базовая функциональность)

---

## 📊 Текущая функциональность

### ✅ Что УЖЕ ЕСТЬ

#### 1. **User Service** (порт 5001)
- ✅ CRUD операции с пользователями (создание, чтение, обновление, удаление)
- ✅ Поля пользователя: имя, email, телефон, адрес
- ✅ Статистика: общее количество, новые сегодня
- ✅ 5 тестовых пользователей

**Endpoints:**
```
GET    /api/users           # Список всех пользователей
GET    /api/users/:id       # Получить пользователя
POST   /api/users           # Создать пользователя
PUT    /api/users/:id       # Обновить пользователя
DELETE /api/users/:id       # Удалить пользователя
GET    /api/users/stats     # Статистика
```

#### 2. **Product Service** (порт 5002)
- ✅ CRUD операции с товарами
- ✅ Поля товара: название, описание, цена, остаток, категория, URL картинки
- ✅ Фильтрация по категории, минимальной/максимальной цене
- ✅ Статистика: кол-во товаров, общий остаток, средняя/мин/макс цена
- ✅ Список уникальных категорий
- ✅ 7 тестовых товаров (Apple продукты)

**Endpoints:**
```
GET    /api/products                    # Список товаров с фильтрами
GET    /api/products/:id                # Получить товар
POST   /api/products                    # Создать товар
PUT    /api/products/:id                # Обновить товар
DELETE /api/products/:id                # Удалить товар
GET    /api/products/stats              # Статистика товаров
GET    /api/products/categories         # Список категорий
```

**Параметры фильтрации:**
- `?category=Телефоны` - по категории
- `?min_price=10000&max_price=50000` - по диапазону цен

#### 3. **Order Service** (порт 5003)
- ✅ CRUD операции с заказами
- ✅ Поддержка нескольких товаров в заказе (order_items)
- ✅ Поля заказа: user_id, контакты, итоговая сумма, статус, способ оплаты, адрес доставки
- ✅ Управление статусом: pending → processing → shipped → delivered (или cancelled)
- ✅ Фильтрация по статусу и пользователю
- ✅ Статистика: кол-во заказов, выручка, средний чек, заказы сегодня
- ✅ 3 тестовых заказа

**Endpoints:**
```
GET    /api/orders                      # Список заказов с фильтрами
GET    /api/orders/:id                  # Получить заказ
POST   /api/orders                      # Создать заказ
PUT    /api/orders/:id/status           # Обновить статус
DELETE /api/orders/:id                  # Удалить заказ
GET    /api/orders/stats                # Статистика заказов
```

**Статусы заказа:**
- `pending` - ожидает обработки
- `processing` - в обработке
- `shipped` - отправлен
- `delivered` - доставлен
- `cancelled` - отменён

---

## ❌ Что ОТСУТСТВУЕТ (критические функции e-commerce)

### 1. **🛒 Корзина покупок (Shopping Cart)**
**Проблема:** Заказы создаются напрямую, нет промежуточного хранения товаров

**Требуется:**
- Таблица `cart_items` с полями: user_id, product_id, quantity, added_at
- Endpoints:
  - `POST /api/cart` - добавить товар в корзину
  - `GET /api/cart/:user_id` - получить корзину пользователя
  - `PUT /api/cart/:item_id` - изменить количество
  - `DELETE /api/cart/:item_id` - удалить из корзины
  - `POST /api/cart/checkout` - оформить заказ из корзины

### 2. **🔐 Аутентификация и авторизация**
**Проблема:** Нет системы входа, паролей, сессий, ролей

**Требуется:**
- Добавить поля в таблицу users: password_hash, role (admin/user), is_active
- JWT tokens или session-based аутентификация
- Endpoints:
  - `POST /api/auth/register` - регистрация
  - `POST /api/auth/login` - вход
  - `POST /api/auth/logout` - выход
  - `GET /api/auth/me` - текущий пользователь
  - `POST /api/auth/change-password` - смена пароля

### 3. **🔍 Поиск товаров**
**Проблема:** Только фильтрация по категории и цене, нет полнотекстового поиска

**Требуется:**
- `GET /api/products/search?q=iPhone` - поиск по названию и описанию
- Поддержка SQLite FTS5 (Full-Text Search)
- Автодополнение поиска

### 4. **⭐ Отзывы и рейтинги**
**Проблема:** Нет возможности оставлять отзывы и оценивать товары

**Требуется:**
- Таблица `reviews`: product_id, user_id, rating (1-5), comment, created_at
- Добавить поле avg_rating в products
- Endpoints:
  - `POST /api/products/:id/reviews` - добавить отзыв
  - `GET /api/products/:id/reviews` - получить отзывы товара

### 5. **❤️ Избранное (Wishlist)**
**Проблема:** Нельзя сохранить товары "на потом"

**Требуется:**
- Таблица `wishlist`: user_id, product_id, added_at
- Endpoints:
  - `POST /api/wishlist` - добавить в избранное
  - `GET /api/wishlist/:user_id` - получить избранное
  - `DELETE /api/wishlist/:id` - удалить из избранного

### 6. **📦 Автоматическое управление остатками**
**Проблема:** Поле stock есть, но остаток не уменьшается при заказе

**Требуется:**
- При создании заказа: автоматически вычитать quantity из product.stock
- Проверка наличия перед созданием заказа
- Резервирование товара при добавлении в корзину (опционально)

### 7. **💳 Интеграция платежей**
**Проблема:** Есть поле payment_method, но нет реальной оплаты

**Требуется:**
- Интеграция с платежными системами (Stripe, ЮКасса, PayPal)
- Таблица `payments`: order_id, amount, status, payment_provider, transaction_id
- Endpoints:
  - `POST /api/payments/create` - создать платеж
  - `GET /api/payments/:order_id/status` - статус оплаты

### 8. **📍 Отслеживание доставки**
**Проблема:** Есть статусы, но нет детальной истории изменений

**Требуется:**
- Таблица `order_tracking`: order_id, status, location, timestamp, notes
- История всех изменений статуса
- Endpoints:
  - `GET /api/orders/:id/tracking` - история отслеживания

### 9. **🖼️ Загрузка изображений**
**Проблема:** Есть поле image_url, но нельзя загружать картинки

**Требуется:**
- Endpoint для загрузки файлов: `POST /api/upload/product-image`
- Хранение в Termux: ~/termux-backend/uploads/
- Генерация миниатюр (thumbnails)
- Поддержка нескольких фото на товар

### 10. **🎟️ Скидки и купоны**
**Проблема:** Нет системы промокодов и акций

**Требуется:**
- Таблица `coupons`: code, discount_type (percent/fixed), discount_value, valid_from, valid_until, max_uses
- Таблица `coupon_usage`: coupon_id, user_id, order_id, used_at
- Endpoints:
  - `POST /api/coupons/validate` - проверить купон
  - `POST /api/orders/apply-coupon` - применить купон к заказу

### 11. **👔 Варианты товаров**
**Проблема:** Один товар - один вариант (нет размеров, цветов и т.д.)

**Требуется:**
- Таблица `product_variants`: product_id, size, color, additional_price, stock
- Endpoint: `GET /api/products/:id/variants`

### 12. **🚚 Расчет стоимости доставки**
**Проблема:** Нет калькуляции доставки

**Требуется:**
- Таблица `shipping_methods`: name, price, estimated_days
- Endpoint: `POST /api/shipping/calculate` - рассчитать доставку по адресу

### 13. **👨‍💼 Админ-панель**
**Проблема:** Нет специальных endpoints для администратора

**Требуется:**
- Middleware для проверки роли admin
- Endpoints:
  - `GET /api/admin/dashboard` - статистика для админа
  - `GET /api/admin/orders/pending` - заказы требующие обработки
  - `PUT /api/admin/products/:id/featured` - выделить товар

### 14. **📧 Уведомления**
**Проблема:** Нет уведомлений о статусе заказа

**Требуется:**
- Email-уведомления (через SMTP или Termux-API)
- Push-уведомления во Flutter app
- События:
  - Заказ создан
  - Статус изменен
  - Оплата получена

### 15. **📈 Расширенная аналитика**
**Проблема:** Есть базовая статистика, но нет детальной аналитики

**Требуется:**
- Топ продаваемых товаров
- График продаж по дням/месяцам
- Анализ поведения пользователей
- Конверсия корзина → заказ

### 16. **🤖 Рекомендации товаров**
**Проблема:** Нет связанных или рекомендуемых товаров

**Требуется:**
- Таблица `product_recommendations`: product_id, recommended_product_id
- Алгоритм: "Часто покупают вместе"
- Endpoint: `GET /api/products/:id/recommendations`

---

## 🚀 План развития (приоритеты)

### 🔴 **Приоритет 1 - КРИТИЧНО** (без этого магазин не работает полноценно)

1. **Корзина покупок** - пользователи должны собирать товары перед оформлением
2. **Аутентификация** - регистрация/вход обязательны для безопасности
3. **Управление остатками** - товары должны заканчиваться при покупке
4. **Поиск товаров** - критично для юзабилити в каталоге

**Время реализации:** 1-2 дня
**Сложность:** Средняя

### 🟠 **Приоритет 2 - ВАЖНО** (нужно для конкурентоспособности)

5. **Отзывы и рейтинги** - влияют на доверие к магазину
6. **Загрузка изображений** - фото товаров должны быть реальными
7. **Скидки и купоны** - важно для маркетинга
8. **Отслеживание доставки** - клиенты хотят знать где их заказ

**Время реализации:** 2-3 дня
**Сложность:** Средняя

### 🟡 **Приоритет 3 - ПОЛЕЗНО** (улучшает UX)

9. **Избранное** - удобно для пользователей
10. **Админ-панель** - упрощает управление магазином
11. **Уведомления** - повышает вовлеченность
12. **Расчет доставки** - прозрачность цен

**Время реализации:** 2-3 дня
**Сложность:** Средняя-высокая

### 🟢 **Приоритет 4 - ДОПОЛНИТЕЛЬНО** (для масштабирования)

13. **Интеграция платежей** - для реальных транзакций
14. **Варианты товаров** - для сложных каталогов
15. **Аналитика** - для бизнес-решений
16. **Рекомендации** - для увеличения среднего чека

**Время реализации:** 3-5 дней
**Сложность:** Высокая

---

## 💡 Рекомендуемый порядок реализации

### **Этап 1: Базовый функционал (Приоритет 1)**

#### 1.1 Корзина покупок
```python
# Добавить в новый файл: termux/services/cart-service.py (порт 5004)

CREATE TABLE cart_items (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    quantity INTEGER DEFAULT 1,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)

# Endpoints:
POST   /api/cart                # Добавить в корзину
GET    /api/cart/:user_id       # Получить корзину
PUT    /api/cart/:id            # Изменить количество
DELETE /api/cart/:id            # Удалить из корзины
POST   /api/cart/checkout       # Оформить заказ
DELETE /api/cart/clear/:user_id # Очистить корзину
```

#### 1.2 Аутентификация
```python
# Обновить: termux/services/user-service.py

# Добавить в таблицу users:
ALTER TABLE users ADD COLUMN password_hash TEXT;
ALTER TABLE users ADD COLUMN role TEXT DEFAULT 'user';
ALTER TABLE users ADD COLUMN is_active INTEGER DEFAULT 1;

# Новые endpoints:
POST /api/auth/register          # Регистрация
POST /api/auth/login             # Вход (возвращает JWT)
GET  /api/auth/me                # Текущий пользователь
POST /api/auth/change-password   # Смена пароля
```

**Библиотеки для Python:**
- `bcrypt` - хеширование паролей
- `PyJWT` - генерация JWT токенов

#### 1.3 Управление остатками
```python
# Обновить: termux/services/order-service.py

def create_order():
    # При создании заказа:
    for item in order_items:
        # Проверить наличие
        product = get_product(item['product_id'])
        if product['stock'] < item['quantity']:
            return error('Недостаточно товара на складе')

        # Уменьшить остаток
        update_product_stock(
            item['product_id'],
            product['stock'] - item['quantity']
        )
```

#### 1.4 Поиск товаров
```python
# Обновить: termux/services/product-service.py

@app.route('/api/products/search', methods=['GET'])
def search_products():
    query = request.args.get('q', '')

    conn = get_db()
    cursor = conn.execute('''
        SELECT * FROM products
        WHERE name LIKE ? OR description LIKE ?
        ORDER BY created_at DESC
    ''', (f'%{query}%', f'%{query}%'))

    products = [dict(row) for row in cursor.fetchall()]
    return jsonify({'products': products, 'total': len(products)})
```

### **Этап 2: Улучшения UX (Приоритет 2)**

#### 2.1 Отзывы и рейтинги
```python
# Новый файл: termux/services/review-service.py (порт 5005)

CREATE TABLE reviews (
    id INTEGER PRIMARY KEY,
    product_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    rating INTEGER CHECK(rating >= 1 AND rating <= 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)

# Endpoints:
POST /api/reviews                    # Создать отзыв
GET  /api/products/:id/reviews       # Отзывы товара
GET  /api/products/:id/rating        # Средний рейтинг
```

#### 2.2 Загрузка изображений
```python
# Обновить: termux/services/product-service.py

import os
from werkzeug.utils import secure_filename

UPLOAD_FOLDER = os.path.expanduser('~/termux-backend/uploads')

@app.route('/api/products/upload-image', methods=['POST'])
def upload_image():
    if 'image' not in request.files:
        return jsonify({'error': 'Нет файла'}), 400

    file = request.files['image']
    filename = secure_filename(file.filename)
    filepath = os.path.join(UPLOAD_FOLDER, filename)
    file.save(filepath)

    return jsonify({
        'success': True,
        'image_url': f'/uploads/{filename}'
    })
```

### **Этап 3: Маркетинг и конверсия (Приоритет 2-3)**

#### 3.1 Система купонов
```python
# Новый файл: termux/services/coupon-service.py (порт 5006)

CREATE TABLE coupons (
    id INTEGER PRIMARY KEY,
    code TEXT UNIQUE NOT NULL,
    discount_type TEXT CHECK(discount_type IN ('percent', 'fixed')),
    discount_value REAL,
    valid_from TIMESTAMP,
    valid_until TIMESTAMP,
    max_uses INTEGER,
    current_uses INTEGER DEFAULT 0
)

# Endpoints:
POST /api/coupons/validate           # Проверить купон
POST /api/orders/apply-coupon        # Применить к заказу
```

### **Этап 4: Административные функции**

#### 4.1 Админ панель
```python
# Обновить все сервисы с middleware для проверки роли

def admin_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        token = request.headers.get('Authorization')
        user = decode_jwt(token)
        if user['role'] != 'admin':
            return jsonify({'error': 'Доступ запрещён'}), 403
        return f(*args, **kwargs)
    return wrapper

@app.route('/api/admin/dashboard', methods=['GET'])
@admin_required
def admin_dashboard():
    return jsonify({
        'total_users': count_users(),
        'total_orders': count_orders(),
        'revenue_today': get_revenue_today(),
        'pending_orders': get_pending_orders()
    })
```

---

## 📱 Обновления Flutter приложения

При добавлении новых функций потребуется обновить:

### 1. **Экран корзины** (`lib/screens/cart_screen.dart`)
- Показать товары в корзине
- Изменить количество
- Удалить из корзины
- Кнопка "Оформить заказ"

### 2. **Экран авторизации** (`lib/screens/auth_screen.dart`)
- Форма входа (email + пароль)
- Форма регистрации
- Хранение JWT токена в SharedPreferences

### 3. **Экран товара** (`lib/screens/product_detail_screen.dart`)
- Кнопка "Добавить в корзину"
- Показать отзывы и рейтинг
- Кнопка "В избранное"

### 4. **Поиск** (`lib/screens/search_screen.dart`)
- Поле поиска с автодополнением
- Список результатов

### 5. **Профиль пользователя** (`lib/screens/profile_screen.dart`)
- История заказов
- Избранное
- Настройки

---

## 📊 Сравнение: сейчас vs после реализации

| Функция | Сейчас | После Этапа 1 | После Этапа 4 |
|---------|--------|---------------|---------------|
| Регистрация/Вход | ❌ | ✅ | ✅ |
| Корзина | ❌ | ✅ | ✅ |
| Поиск | ❌ | ✅ | ✅ |
| Остатки | ⚠️ (не работает) | ✅ | ✅ |
| Отзывы | ❌ | ❌ | ✅ |
| Купоны | ❌ | ❌ | ✅ |
| Изображения | ⚠️ (только URL) | ❌ | ✅ |
| Админка | ❌ | ❌ | ✅ |
| Платежи | ❌ | ❌ | ⚠️ (базово) |
| Аналитика | ⚠️ (базовая) | ⚠️ (базовая) | ✅ |

---

## 🎯 Следующие шаги

### Рекомендация:

**Начать с Этапа 1 (Приоритет 1)**

1. Создать cart-service.py для корзины
2. Добавить аутентификацию в user-service.py
3. Реализовать управление остатками в order-service.py
4. Добавить поиск в product-service.py
5. Обновить Flutter приложение с новыми экранами

**Результат:** Получится полноценный интернет-магазин с базовыми функциями.

---

## 📝 Заключение

**Текущее состояние:**
- ✅ Основа работает (CRUD для users/products/orders)
- ⚠️ Это MVP, но не полноценный e-commerce

**Потенциал развития:**
- 🚀 16 критичных функций для добавления
- 📈 Можно развивать в 4 этапа по приоритетам
- 💪 Архитектура поддерживает масштабирование (микросервисы + SQLite)

**Рекомендация:**
Начать с **Приоритета 1** (корзина + аутентификация + остатки + поиск) - это даст 80% функциональности полноценного магазина при 20% усилий.

---

**Подготовил:** Claude Code
**Архитектура:** Flutter 3.24.5 + Termux Flask (Python 3.11) + SQLite
**GitHub:** svend4/daten30
