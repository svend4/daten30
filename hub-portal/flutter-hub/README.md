# 📱 Dynamic Hub - Flutter App

Универсальное Flutter приложение с динамическим подключением микросервисов.

---

## 🎯 Описание

Dynamic Hub - это Flutter приложение, которое работает как **скелет** (рамка), автоматически обнаруживая и отображая доступные микросервисы из Termux.

### Ключевые возможности

- ✅ **Автоматическое обнаружение сервисов** через Service Registry
- ✅ **Динамический UI** - интерфейс генерируется на основе UI schemas
- ✅ **Plug and Play** - добавил сервис в Termux → появился в приложении
- ✅ **Offline индикатор** - показывает статус подключения
- ✅ **Auto-refresh** - обновление списка сервисов каждые 30 секунд
- ✅ **Нет хардкода** - всё настраивается через Service Registry

---

## 🚀 Быстрый старт

### 1. Собрать APK

```bash
cd ~/daten30/hub-portal/flutter-hub
bash build-apk.sh
```

### 2. Установить на устройство

```bash
adb install build/app/outputs/flutter-apk/app-release.apk
```

### 3. Запустить микросервисы

```bash
cd ~/daten30/hub-portal
bash scripts/start-all.sh
```

### 4. Открыть приложение

Запустить "Dynamic Hub" на Android устройстве.

---

## 📂 Структура проекта

```
flutter-hub/
├── lib/
│   └── main.dart                  # Весь код приложения (600+ строк)
├── pubspec.yaml                   # Зависимости
├── build-apk.sh                   # Скрипт сборки
├── BUILD_INSTRUCTIONS.md          # Детальная инструкция
└── README.md                      # Этот файл
```

---

## 🏗️ Архитектура

```
┌──────────────────────────────────┐
│  Flutter App (Dynamic Hub)       │
│  - ServiceDiscovery              │
│  - DynamicWidgetBuilder          │
│  - Auto-refresh (30s)            │
└──────────────────────────────────┘
           ↕ HTTP
┌──────────────────────────────────┐
│  Service Registry (port 5000)    │
│  - Список сервисов               │
│  - UI schemas                    │
│  - Health checks                 │
└──────────────────────────────────┘
           ↕
┌──────────────────────────────────┐
│  Микросервисы (5001-5005)        │
│  🛒 Products  🌤️ Weather          │
│  ₿ Crypto    📰 News             │
│  ✅ Tasks                         │
└──────────────────────────────────┘
```

---

## 💡 Как это работает

### 1. Обнаружение сервисов

```dart
// ServiceDiscovery опрашивает Registry
Future<List<MicroService>> discoverServices() async {
  final response = await http.get(
    Uri.parse('http://127.0.0.1:5000/api/services')
  );

  // Парсит ответ и возвращает список сервисов
  return services;
}
```

### 2. Отображение карточек

```dart
// Для каждого сервиса создаётся карточка
ServiceCard(
  service: service,
  onTap: () {
    // Открыть экран сервиса
    Navigator.push(context, DynamicServiceScreen(service));
  },
)
```

### 3. Динамический UI

```dart
// DynamicWidgetBuilder генерирует UI из schema
Widget build(Map<String, dynamic> schema, Map<String, dynamic> data) {
  switch (schema['type']) {
    case 'list':
      return ListView(...);  // Автоматически!
    case 'card':
      return Card(...);      // Автоматически!
  }
}
```

---

## 🎨 Компоненты

### HubHomeScreen

Главный экран с карточками сервисов:
- Grid layout (2 колонки)
- Индикатор подключения (Online/Offline)
- Pull-to-refresh
- Auto-refresh каждые 30 секунд
- Error handling

### ServiceCard

Карточка отдельного сервиса:
- Иконка из UI schema
- Название и описание
- Цвет темы
- Номер порта

### DynamicServiceScreen

Экран конкретного сервиса:
- Загрузка данных с endpoint
- Рендеринг через DynamicWidgetBuilder
- Refresh кнопка
- Error states

### DynamicWidgetBuilder

Генератор UI из JSON schema:
- Поддержка типов: list, card, grid
- Template interpolation ({{variable}})
- Автоматический парсинг данных

---

## 🔧 Конфигурация

### Изменить URL Service Registry

Редактировать `lib/main.dart`:

```dart
class ServiceDiscovery {
  static const String registryUrl = 'http://127.0.0.1:5000';
}
```

### Добавить новый тип UI

Редактировать `DynamicWidgetBuilder`:

```dart
Widget build(Map schema, Map data) {
  switch (schema['type']) {
    case 'table':  // Новый тип!
      return _buildTable(schema, data);
  }
}
```

---

## 📱 Поддерживаемые платформы

- ✅ Android (основная платформа)
- ⚠️ iOS (требует настройка Xcode)
- ⚠️ Web (частично)
- ⚠️ Desktop (частично)

---

## 🐛 Troubleshooting

### Приложение показывает "Registry недоступен"

**Причина:** Service Registry не запущен

**Решение:**
```bash
cd ~/daten30/hub-portal
bash scripts/start-all.sh
bash scripts/health-check.sh
```

### Нет карточек сервисов

**Причина:** Микросервисы не запущены или не зарегистрировались

**Решение:**
```bash
# Проверить Registry
curl http://127.0.0.1:5000/api/services

# Перезапустить сервисы
bash scripts/stop-all.sh
bash scripts/start-all.sh
```

### "Cleartext HTTP not permitted"

**Причина:** Android блокирует HTTP

**Решение:** Убедитесь что `network_security_config.xml` создан и подключен

---

## 📊 Технологии

- **Flutter:** 3.24.5+
- **Dart:** 3.0+
- **Dependencies:**
  - http: ^1.1.0 (HTTP клиент)
  - Material Design 3

---

## 🔮 Планы развития

- [ ] WebSocket для real-time обновлений
- [ ] Кэширование списка сервисов
- [ ] Настройки пользователя
- [ ] Dark theme
- [ ] Поиск по сервисам
- [ ] Избранные сервисы
- [ ] Push уведомления

---

## 📄 Лицензия

MIT License

---

## 👥 Авторы

Claude Code Development Team

---

**Готово! Динамическое приложение готово к использованию! 🎉**
