# 🚀 Автоматическая сборка Dynamic Hub App

## Быстрый старт

### Вариант 1: Автоматическая сборка (рекомендуется)

Этот скрипт автоматически установит Flutter (если нужно) и соберет APK:

```bash
cd ~/daten30/hub-portal/flutter-hub
bash auto-build.sh
```

Скрипт выполнит:
- ✅ Проверку Flutter (установит если нет)
- ✅ Настройку Flutter для Android
- ✅ Установку зависимостей
- ✅ Создание иконок
- ✅ Сборку APK
- ✅ Копирование APK в удобное место

### Вариант 2: Обычная сборка (если Flutter уже установлен)

```bash
cd ~/daten30/hub-portal/flutter-hub
bash build-apk.sh
```

### Вариант 3: Ручная сборка

```bash
cd ~/daten30/hub-portal/flutter-hub
flutter pub get
flutter build apk --release
```

---

## 📦 Результат сборки

После успешной сборки APK будет находиться в двух местах:

1. **Оригинальный APK:**
   ```
   build/app/outputs/flutter-apk/app-release.apk
   ```

2. **Копия с удобным именем:**
   ```
   DynamicHub-release.apk
   ```

---

## 📱 Установка APK

### Через ADB (Android Debug Bridge)

```bash
adb install DynamicHub-release.apk
```

### В Termux

```bash
# Копировать в Downloads
cp DynamicHub-release.apk ~/storage/downloads/

# Открыть файловый менеджер и установить APK
```

### На Desktop

```bash
# Скопировать на телефон
adb push DynamicHub-release.apk /sdcard/Download/

# Или через USB как обычный файл
```

---

## 🔧 Требования

### Минимальные

- **Android:** 7.0+ (API 21+)
- **RAM:** 2 GB
- **Место на диске:** 100 MB для APK, 1 GB для сборки

### Для сборки в Termux

```bash
pkg install git wget
```

### Для сборки на Desktop

- Git
- Wget или curl
- Android SDK (автоматически установится через Flutter)

---

## 🛠️ Что делает auto-build.sh

1. **Проверка окружения**
   - Определяет Termux или Linux
   - Проверяет наличие Flutter

2. **Установка Flutter** (если нет)
   - Клонирует Flutter SDK
   - Настраивает PATH
   - Принимает Android лицензии

3. **Настройка проекта**
   - Создает иконки-заглушки
   - Устанавливает зависимости
   - Настраивает Android SDK

4. **Сборка APK**
   - Запускает `flutter build apk`
   - Создает копию APK с удобным именем
   - Показывает размер и расположение

---

## ⚡ Быстрые команды

### Пересборка без установки Flutter

```bash
flutter clean
flutter pub get
flutter build apk --release
```

### Только обновить зависимости

```bash
flutter pub get
```

### Проверить Flutter

```bash
flutter doctor -v
```

### Очистить кэш

```bash
flutter clean
```

---

## 🐛 Troubleshooting

### Ошибка: Flutter not found

**Решение:**
```bash
# Запустить auto-build.sh - он установит Flutter автоматически
bash auto-build.sh
```

### Ошибка: Android licenses not accepted

**Решение:**
```bash
flutter doctor --android-licenses
# Нажать 'y' на все вопросы
```

### Ошибка: Gradle build failed

**Решение:**
```bash
# Очистить и пересобрать
flutter clean
rm -rf build/
flutter pub get
flutter build apk --release
```

### Ошибка: SDK location not found

**Решение:**
```bash
# Создать local.properties
echo "sdk.dir=$ANDROID_HOME" > android/local.properties
echo "flutter.sdk=$HOME/flutter" >> android/local.properties
```

### APK слишком большой

**Решение:**
```bash
# Собрать split APKs (отдельно для каждой архитектуры)
flutter build apk --split-per-abi
# APKs будут в build/app/outputs/flutter-apk/
```

---

## 📊 Размеры APK

| Тип сборки | Размер | Описание |
|------------|--------|----------|
| **Release** | ~20-30 MB | Оптимизированный для продакшн |
| **Debug** | ~50-80 MB | С отладочной информацией |
| **Split-per-ABI** | ~15-20 MB | Отдельно для ARM, ARM64, x86 |

---

## 🔄 Обновление Flutter

### Обновить Flutter до последней версии

```bash
flutter upgrade
```

### Переключиться на stable канал

```bash
flutter channel stable
flutter upgrade
```

---

## 📝 Структура проекта

```
flutter-hub/
├── lib/
│   └── main.dart                         # Код приложения (600+ строк)
├── android/                              # Android конфигурация
│   ├── app/
│   │   ├── build.gradle                  # Gradle конфигурация
│   │   └── src/main/
│   │       ├── AndroidManifest.xml       # Манифест с INTERNET permission
│   │       ├── kotlin/                   # MainActivity
│   │       └── res/
│   │           ├── xml/
│   │           │   └── network_security_config.xml  # HTTP доступ
│   │           └── values/
│   │               └── styles.xml
│   ├── build.gradle                      # Project-level gradle
│   ├── settings.gradle
│   └── gradle.properties
├── pubspec.yaml                          # Зависимости
├── auto-build.sh                         # Автоматическая сборка
├── build-apk.sh                          # Обычная сборка
├── BUILD_INSTRUCTIONS.md                 # Детальные инструкции
└── AUTO_BUILD_README.md                  # Этот файл
```

---

## 🎯 Следующие шаги

После сборки APK:

1. **Установите APK на устройство**
2. **Запустите Hub Portal backend в Termux:**
   ```bash
   cd ~/daten30/hub-portal
   bash scripts/start-all.sh
   ```
3. **Откройте приложение** - оно автоматически найдет все сервисы!

---

## 💡 Советы

### Ускорение сборки

1. **Используйте --release только для финальной версии**
   ```bash
   flutter build apk  # Debug сборка - быстрее
   ```

2. **Кэшируйте Gradle зависимости**
   - После первой сборки следующие будут быстрее

3. **Увеличьте память для Gradle**
   - Отредактируйте `android/gradle.properties`:
     ```
     org.gradle.jvmargs=-Xmx4G
     ```

### Оптимизация APK

1. **Split по архитектуре**
   ```bash
   flutter build apk --split-per-abi
   ```

2. **Удалить неиспользуемые ресурсы**
   - В `android/app/build.gradle`:
     ```gradle
     buildTypes {
         release {
             shrinkResources true
             minifyEnabled true
         }
     }
     ```

---

## 📚 Дополнительная документация

- [BUILD_INSTRUCTIONS.md](BUILD_INSTRUCTIONS.md) - Детальные инструкции по сборке
- [README.md](README.md) - Описание приложения
- [../FLUTTER_APPS_COMPARISON.md](../../FLUTTER_APPS_COMPARISON.md) - Сравнение вариантов

---

**Создано:** 2026-01-06
**Версия:** 1.0
**Проект:** Daten30 Dynamic Hub
