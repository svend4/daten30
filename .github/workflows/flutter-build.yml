name: Build Flutter APK

on:
  push:
    branches: [ main, develop, claude/** ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # Ручной запуск

jobs:
  build-apk:
    name: Build Flutter APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'
          cache: true

      - name: Get dependencies
        working-directory: demo-app/mobile-flutter
        run: flutter pub get

      - name: Create Android platform files
        working-directory: demo-app/mobile-flutter
        run: |
          if [ ! -d "android" ]; then
            echo "Android folder not found, creating..."
            flutter create --platforms android --org com.demoapp --project-name demo_app_mobile .
          else
            echo "Android folder already exists"
          fi

      - name: Enable cleartext traffic for localhost
        working-directory: demo-app/mobile-flutter/android/app/src/main
        run: |
          echo "=== Enabling cleartext HTTP for localhost ==="

          # Add usesCleartextTraffic to AndroidManifest.xml
          sed -i 's/android:label="demo_app_mobile"/android:label="demo_app_mobile"\n        android:usesCleartextTraffic="true"/' AndroidManifest.xml

          # Create network security config
          mkdir -p res/xml
          cat > res/xml/network_security_config.xml <<'EOF'
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
EOF

          # Reference it in AndroidManifest.xml
          sed -i 's/android:usesCleartextTraffic="true"/android:usesCleartextTraffic="true"\n        android:networkSecurityConfig="@xml\/network_security_config"/' AndroidManifest.xml

          echo "✅ Cleartext traffic enabled for localhost"
          echo "=== Updated AndroidManifest.xml ==="
          cat AndroidManifest.xml

      - name: Update Gradle wrapper to fix Maven 403 errors
        working-directory: demo-app/mobile-flutter/android
        run: |
          echo "=== Updating Gradle configuration ==="

          # Update gradle-wrapper.properties to use Gradle 8.3
          sed -i 's|distributionUrl=.*|distributionUrl=https\\://services.gradle.org/distributions/gradle-8.3-bin.zip|' gradle/wrapper/gradle-wrapper.properties
          echo "✓ Updated Gradle wrapper to 8.3"
          cat gradle/wrapper/gradle-wrapper.properties

          # Backup original build.gradle
          echo "=== Original build.gradle ==="
          cat build.gradle
          cp build.gradle build.gradle.backup

          # Copy pre-configured build.gradle
          echo "=== Copying new build.gradle ==="
          ls -la ../../../.github/android-build.gradle
          cp ../../../.github/android-build.gradle build.gradle

          # Verify the copy worked
          echo "=== New build.gradle content ==="
          cat build.gradle

          # Clean Gradle cache
          rm -rf ~/.gradle/caches/
          rm -rf .gradle/
          echo "✓ Cleaned Gradle cache"

      - name: Analyze code
        working-directory: demo-app/mobile-flutter
        run: flutter analyze
        continue-on-error: true

      - name: Build APK
        working-directory: demo-app/mobile-flutter
        run: flutter build apk --release

      - name: Get version
        id: version
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "branch=$(echo ${GITHUB_REF#refs/heads/} | sed 's/\//-/g')" >> $GITHUB_OUTPUT

      - name: Rename APK
        run: |
          mv demo-app/mobile-flutter/build/app/outputs/flutter-apk/app-release.apk \
             demo-app/mobile-flutter/build/app/outputs/flutter-apk/demo-app-${{ steps.version.outputs.branch }}-${{ steps.version.outputs.sha_short }}.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-apk-${{ steps.version.outputs.branch }}-${{ steps.version.outputs.sha_short }}
          path: demo-app/mobile-flutter/build/app/outputs/flutter-apk/*.apk
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: demo-app/mobile-flutter/build/app/outputs/flutter-apk/*.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment PR with APK download link
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          BRANCH: ${{ steps.version.outputs.branch }}
          SHA_SHORT: ${{ steps.version.outputs.sha_short }}
        with:
          script: |
            const runId = context.runId;
            const repoUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}`;
            const downloadUrl = `${repoUrl}/actions/runs/${runId}`;
            const branch = process.env.BRANCH;
            const shaShort = process.env.SHA_SHORT;

            const message = [
              '## 📱 Flutter APK собран успешно!',
              '',
              '**Скачать APK:**',
              downloadUrl,
              '',
              '**Файл:** `demo-app-' + branch + '-' + shaShort + '.apk`',
              '',
              '**Режим:** Termux (localhost:5001/5002/5003)',
              '',
              '### Установка:',
              '1. Скачайте APK из артефактов',
              '2. Установите на Android',
              '3. Убедитесь что Termux backend запущен',
              '4. Откройте приложение'
            ].join('\n');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });

  build-info:
    name: Build Information
    runs-on: ubuntu-latest
    needs: build-apk

    steps:
      - name: Build Summary
        run: |
          echo "## 🎉 Flutter APK Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ APK успешно собран!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Детали:" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Flutter:** 3.24.5" >> $GITHUB_STEP_SUMMARY
          echo "- **API Mode:** Termux (localhost)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Скачать APK:" >> $GITHUB_STEP_SUMMARY
          echo "Перейдите в Actions → Artifacts" >> $GITHUB_STEP_SUMMARY
